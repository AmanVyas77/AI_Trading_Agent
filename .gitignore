# ── Secrets ──────────────────────────────────────────────────
.env

# ── Python ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg

# ── Virtual Environments ─────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Data (raw & large files — keep processed outputs) ────────
data/raw/
data/quant_research.db
*.parquet
*.csv
*.feather
*.h5

# ── Frozen data vintages ─────────────────────────────────────
# backtests/vintage_*/ holds byte-for-byte snapshots used to make diagnostic
# runs reproducible while the nightly news backfill keeps revising history.
# The .db is ~640 MB — well over GitHub's 100 MB limit. Ignore the payload,
# but ALWAYS commit MANIFEST.json: it carries the per-file sha256s, so a
# restored snapshot can be verified against the run that used it.
backtests/vintage_*/*.db
backtests/vintage_*/*.sqlite
backtests/vintage_*/*.sqlite3

# ── Notebooks checkpoints ────────────────────────────────────
.ipynb_checkpoints/

# ── Logs ─────────────────────────────────────────────────────
logs/*.log

# ── OS ───────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── IDE ──────────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── Models (large binary weights) ────────────────────────────
*.pt
*.pkl
*.bin
models/

# ── Backtest artifacts (keep reports, exclude raw result dumps)
backtests/results/*.pkl
.pytest_cache/

# ── Phase 4: RAG artifacts ────────────────────────────────────
data/chroma_db/              # ChromaDB vector store (large binary, rebuilt via --ingest)
research_papers/**/*.pdf     # downloaded PDFs (reproducible via scripts/download_papers.py)

# ── Research wiki session outputs (keep structure + seed notes)
research_wiki/research/
research_wiki/companies/
